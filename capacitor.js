// This file is a placeholder. When Capacitor builds the Android app,
// it automatically injects the real native bridge and plugin JS
// (capacitor.js, cordova.js, plugin bundles) into the compiled www/
// output. We reference window.Capacitor?.Plugins in app.js and guard
// every call, so the app also runs fine in a plain browser during
// development where these globals won't exist.
